@model Ecommerce.Models.Order

@{
    ViewData["Title"] = "Edit Order";
}

<div class="container py-4">
    <h2 class="mb-4 text-primary">Edit Order #@Model.OrderId</h2>

    <div class="card shadow-sm">
        <div class="card-body">
            <form asp-action="Edit" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="OrderId" />

                <!-- Customer Info (readonly) -->
                <div class="mb-3">
                    <label class="form-label">Customer Name</label>
                    <input class="form-control" asp-for="CustomerName" readonly />
                </div>

                <div class="mb-3">
                    <label class="form-label">Customer Email</label>
                    <input class="form-control" asp-for="CustomerEmail" readonly />
                </div>

                <div class="mb-3">
                    <label class="form-label">Customer Address</label>
                    <input class="form-control" asp-for="CustomerAddress" readonly />
                </div>

                <div class="mb-3">
                    <label class="form-label">Customer Phone</label>
                    <input class="form-control" asp-for="CustomerPhone" readonly />
                </div>

                <!-- Order Amounts -->
                <div class="mb-3">
                    <label class="form-label">Subtotal</label>
                    <input class="form-control" value="৳@(Model.TotalAmount - Model.DeliveryCharge).ToString(" N0")" readonly />
                </div>

                <div class="mb-3">
                    <label class="form-label">Delivery Charge</label>
                    <input class="form-control" value="৳@Model.DeliveryCharge.ToString("N0")" readonly />
                </div>

                <div class="mb-3">
                    <label class="form-label">Total Amount</label>
                    <input class="form-control" value="৳@Model.TotalAmount.ToString("N0")" readonly />
                </div>

                <!-- Order Status -->
                <div class="mb-3">
                    <label for="Status">Order Status</label>
                    @Html.DropDownListFor(model => model.Status,
                    new SelectList(new List<string> {
                                        "Pending", "Processing", "Shipped", "OutForDelivery", "Delivered", "Cancelled"
                                        }, Model.Status),
                                        new { @class = "form-control" })
                </div>

                <!-- Actions -->
                <div class="d-flex justify-content-between">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save"></i> Save Changes
                    </button>
                    <a asp-action="Manage" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Back to Orders
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>
